{
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "gen-pkg": "node scripts/gen-pkg.js"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "turbo": "^1.13.2"
  },
  "dependencies": {
    "@testeed/test": "^1.0.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
